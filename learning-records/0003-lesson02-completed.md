# Lesson 0002 完成:变量声明与基本类型系统

## 练习 1

`StudentRecord.java` 五个变量类型声明和字面量全部正确(含 `char grade = 'A'`)。唯一偏差:题目要求 5 个独立 `println`,用户写成拼成一个字符串一起打印——功能上能跑,但没有照题目要求执行,已指出。

## 练习 2(找错:4 行变量声明里哪几行编译不过)

- `char letter = "A"``boolean active = 1` 两处判断正确。
- `double price = 19` 判断错误——用户认为这行也编译不过,原因是把 `int` 字面量赋给 `double` 想成了"类型不匹配"。
- 引导用户自己在 IDEA 敲一遍验证,确认编译通过;进一步追问"为什么"时,用户第一次说反了因果("int 包含 double"),纠正为"double 范围比 int 大,int 可以无损装进 double,是单向的自动拓宽转换(widening)"。
- 追加提问 `long total = 100;` 验证泛化能力,用户正确答出"long 范围比 int 大得多,100 能被装下",说明拓宽转换的规则已经迁移到新场景,不是死记硬背这一个例子。

## Implications

- 用户对"数值类型自动拓宽转换"(int→long→float→double 单向兼容)已经建立正确的因果理解,可以泛化到没见过的组合(long),不需要再单独复习。
- 用户偶尔会忽略题目的具体格式要求(比如"5 行分别打印"被写成"拼一行打印")——功能等价但不满足验收条件,提醒需要更仔细读题目的验证标准,不只满足"能跑"。
- `char`/`boolean` 与数值类型转换规则不相干的原因,用户能自己给出准确解释(语法层面不兼容 / 根本不是数值类型),说明基本类型体系的边界已经清楚。
- IDE 环境问题(exercises 目录未标记 Sources Root)已解决,`StudentRecord.java` 顺利创建并带有 `package lesson02;`,后续练习不用再处理这个问题。
