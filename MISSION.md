# Mission: Java 智能应用开发

参考路线:[Java 智能应用开发省时路线(B站)](https://www.bilibili.com/video/BV116drBaEAe/)

## Why
用户维护一个 Vue + TypeScript + FastAPI 的项目,后端代码基本由 AI 代写,看得懂但写不出来。目标是真正掌握 Java 和 Spring Boot,**以 Java/Spring Boot 作为智能应用的主后端**,能够独立完成从接口开发到模型调用的完整链路,而不是成为传统 Java 全家桶开发者。

最终架构方向:Spring Boot(核心业务 + AI 编排调度)+ Python/FastAPI(数据处理、模型实验、生态工具),不追求"全面迁移"。

## 智能应用最小骨架
```
用户请求 → Java/Spring Boot 接口 → 业务与权限 → 数据库/Redis → 模型 API 或 Python 服务 → 流式结果 → 日志、监控与部署
```
模型调用只是其中一个节点。真正可交付的智能应用还需要用户身份、业务数据、失败重试、成本控制、结果存储和可部署性。

## Success looks like

### Java 基础(地基,不能跳过)
- 能不借助 AI,独立读写 Java 面向对象代码(类、接口、继承、多态、封装)
- 熟练使用集合、泛型、Stream、异常处理,写出符合 Java 习惯的代码
- 会用 Maven 管理依赖和项目结构,理解常见生命周期命令
- 会写并运行 JUnit 测试,把测试作为验证手段而不是可选项
- 理解基本并发(线程、线程池、常见线程安全问题)和 JVM 基础(内存模型、GC 概念),足以排查线上真实问题

### Spring Boot 服务能力(最小闭环)
- 能独立搭建 Spring Boot 项目,实现 REST API、数据库访问(Spring Data JPA)、参数校验、异常处理、基本的鉴权
- 能为接口生成 Swagger/接口文档,让前端或 AI 调用方能看懂

### 模型接入能力(智能应用的灵魂)
- 能通过 HTTP 调用大模型 API(普通回答、流式输出)、处理超时与错误重试
- 理解提示词模板、会话记录、文件上传等模型调用常见场景在 Java 侧怎么实现
- 能设计 Java 主后端 + Python 模型服务的协作边界,知道什么放哪边

### 运行环境与交付能力
- Linux 基本命令,能在 Linux 环境运行服务
- Redis 基础:缓存键设计、过期策略、缓存失效,能为一个高频读取场景加入 Redis
- Docker:能用 Dockerfile 打包自己的服务并跑起来
- 能记录一次故障和修复过程(比如缓存未命中、模型超时、容器环境变量错误)

### 项目输出(检验是否真会了)
- 完成一个可演示的智能应用(如知识库问答、文档总结、内容生成后台),从接口到模型调用到部署全链路自己写的比例超过 70%
- 用项目复盘判断是否真的掌握,而不是按播放列表推进

### 后端通用基础(穿插讲解,不开单独课程)
- RESTful API 设计规范
- 常见性能问题(内存泄漏、连接池耗尽)的成因和排查思路
- 常见安全问题(SQL 注入、XSS、CSRF、鉴权漏洞)及其防御方式
- Redis 典型用法(缓存、分布式锁、限流、会话存储)

## 哪些内容不必优先深挖
- 仅为完成课程而学、与智能应用目标无关的 GUI 桌面技术或陈旧页面技术
- 在没有项目需求时,为每个 Spring 生态组件、微服务组件或设计模式单独开坑
- 只记 API、不写接口、不部署项目的"收藏式学习"
- Spring Cloud 微服务全家桶等项目实际需要时再补

## Constraints
- 已完成/在学 TypeScript,会 Python、Vue,有数据库和前后端项目经验,但从未系统学过 Java——通用编程概念可以快速过
- 学习方式:手动敲代码、闭卷编写、先给报错解释再给最小提示、不主动给完整代码、检测方式多样(读代码/预测结果/改错/闭卷编码/小设计题)
- 每节课只讲一个范围明确的知识点,配合可自动验证的练习
- 需要间隔复习和交错练习,根据实际表现动态调整难度
- 练习场景优先贴近后端真实业务:课程、用户、权限、学习记录、模型调用等
- 优先使用现代 Java 官方文档和高可信资料
- 环境:Windows 11 + Git Bash,Eclipse Temurin JDK 21(LTS),Apache Maven,IntelliJ IDEA

## Out of scope
- 暂不深入 JVM 调优、GC 算法细节等高级性能主题(先建立概念性理解即可)
- 暂不学习其他 JVM 语言(Kotlin、Scala)
- 模型训练、微调等 ML 工程内容继续留在 Python 侧
